{% extends 'base.html.twig' %}
{% block body %}
    <table class="table table-bordered">
        <thead>
            <tr class="text-center">
                <td>
                    #
                </td>
                <td>
                    Title
                </td>
                <td>
                    First Name
                </td>
                <td>
                    Last Name
                </td>
                <td>
                    Telephone
                </td>
                <td>
                    Message
                </td>
            </tr>
        </thead>
        <tbody>
            {% for contact in contacts %}
                <tr>
                    <td>
                        <a class="text-decoration-none" href="{{ path('app_manage_contact', {id: contact.id}) }}">
                            {{ contact.id }}
                        </a>
                    </td>
                    <td>
                        {{ contact.title }}
                    </td>
                    <td>
                        {{ contact.firstName }}
                    </td>
                    <td>
                        {{ contact.lastName }}
                    </td>
                    <td>
                        {{ contact.telephone }}
                    </td>
                    <td>
                        {{ contact.message }}
                    </td>
                </tr>

            {% endfor %}
        </tbody>
    </table>
    {% include 'paginator/paginator.html.twig' %}
{% endblock %}
